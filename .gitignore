# Built binary and GoReleaser artifacts
/dashsync
/dist/

# Coverage and profiles
coverage.out
coverage.html
*.prof

# Go workspace — local-only, never committed
go.work
go.work.sum

# Editors and OS
.DS_Store
.idea/
.vscode/

# Local overrides for Claude Code settings
.claude/settings.local.json
